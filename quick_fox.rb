class Fox
  def initialize(name)
  end
    def jumped_over (x, y)
  end
end

quick_fox = Fox.new(quick_fox)

class Arbitrary_object
  def initialize(name)
  end
end

lazy_dog = Arbitrary_object.new(lazy_dog)
daisy_log = Arbitrary_object.new(daisy_log)

quick_fox.jumped_over(lazy_dog, daisy_log)


# def jumped_over(x, y)
#   [x, y]
# end
